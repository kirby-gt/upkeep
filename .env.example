# This file is here for reference only — docker-compose.yml has its own
# credentials hardcoded for local dev (see below), and apps/api reads its
# real config from apps/api/.env (copy apps/api/.env.example to start).

POSTGRES_USER=upkeep
POSTGRES_PASSWORD=upkeep_dev_password
POSTGRES_DB=upkeep
